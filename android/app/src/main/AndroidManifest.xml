<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Isenção de otimização de bateria: sem isso, requestIgnoreBatteryOptimization()
         não abre o diálogo direto (ROMs agressivos matam o foreground service e a
         nav some em background). POST_NOTIFICATIONS já vem do manifest do plugin. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <application
        android:label="truck_router"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${googleMapsApiKey}"/>
        <!-- Impeller OFF (volta pro Skia/GLES). Medido em device (Redmi Note 11,
             Adreno 610): com Impeller/Vulkan a platform view do google_maps_flutter
             trasha o compositor — ~7.5k erros 'unrecognized IGraphicBufferProducer'
             por sessão de navegação = lag de render real da seta/mapa. Com Skia
             cai a ~0. O opt-out é "deprecated" no Flutter atual mas funciona;
             revisitar quando o combo Impeller+platform view amadurecer ou ao migrar. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false"/>
        <!-- launchMode=singleTask é O QUE FAZ O BOTÃO VOLTAR DEVOLVER PRO WHATSAPP.
             Relato do Gilberto em 12/08/2026 (4ª vez do mesmo bug): abrir uma
             localização compartilhada prendia o WhatsApp — tocar no ícone dele
             trazia o Truck Router. Causa: o WhatsApp faz startActivity SEM
             FLAG_ACTIVITY_NEW_TASK, então com singleTop o Android empilha a
             MainActivity DENTRO da task do WhatsApp. taskAffinity="" não protege
             disso: affinity só é consultada quando há NEW_TASK ou launchMode
             singleTask/singleInstance. singleTask garante que a activity é sempre
             raiz da PRÓPRIA task, venha o intent de onde vier.

             O taskAffinity="" saiu junto de propósito: com affinity nula o
             singleTask não reencontra a task existente (a comparação em
             findTaskLocked exige affinity não-nula dos dois lados) e cada link
             abriria uma task nova. Custo assumido: perde-se a mitigação de task
             hijacking (StrandHogg) que o template do Flutter traz — exige app
             malicioso já instalado, e o app não guarda credencial (auth é anônima).

             As 4 tentativas anteriores de corrigir isso foram todas em Dart
             (dedup de URI, classifyMapsUri, PopScope) — nenhuma tocava a task,
             que é onde o bug mora. NÃO voltar pra singleTop. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="geo"/>
            </intent-filter>
            <!-- Localização/rota compartilhada (WhatsApp etc). maps.google.com e o
                 shortlink maps.app.goo.gl: qualquer path (inclui /?q= na raiz). -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"/>
                <data android:host="maps.google.com"/>
                <data android:host="maps.app.goo.gl"/>
            </intent-filter>
            <!-- www.google.com/maps e google.com/maps: restrito a /maps pra não
                 aparecer no chooser de QUALQUER link google.com. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"/>
                <data android:host="www.google.com" android:pathPrefix="/maps"/>
                <data android:host="google.com" android:pathPrefix="/maps"/>
            </intent-filter>
        </activity>
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:stopWithTask="false"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="waze"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="comgooglemaps"/>
        </intent>
    </queries>
</manifest>
